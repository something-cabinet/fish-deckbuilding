/** Hard ceiling drawCards burns above — always >= DEFAULT_HAND_SIZE. */
export const DEFAULT_HAND_MAX = 8
/** Opening hand size, and how many cards a fresh turn's hand is redealt to. */
export const DEFAULT_HAND_SIZE = 5
