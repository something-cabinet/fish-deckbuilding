/**
 * Fallback board size. Real dimensions live on GameState (`cols`/`rows`) so a
 * stage can define its own grid; these are the defaults a stage-less battle uses.
 */
export const DEFAULT_COLS = 9 // A..I
export const DEFAULT_ROWS = 5 // 1..5
