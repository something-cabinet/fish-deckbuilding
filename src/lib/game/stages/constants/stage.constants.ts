/** Default grid a new stage starts at — the size every battle used before stages. */
export const STAGE_DEFAULT_COLS = 9
export const STAGE_DEFAULT_ROWS = 5

/** Authoring bounds. Wide enough to stay readable, small enough to stay playable. */
export const STAGE_MIN_COLS = 3
export const STAGE_MAX_COLS = 12
export const STAGE_MIN_ROWS = 3
export const STAGE_MAX_ROWS = 8
