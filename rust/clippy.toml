# ── Clippy Configuration ──────────────────────────────────────────────────────
# Per wiki-mem convention: curated list, not all = warn.

# Allow unwrap in test code only — production unwrap still warned
allow-unwrap-in-tests = true

# MSRV for lints
msrv = "1.81"

# Doc-comment requirements — opt in for now
# avoid-breaking-exported-api = false
