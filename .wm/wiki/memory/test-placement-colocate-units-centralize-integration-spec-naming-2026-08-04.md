---
title: Test placement — colocate units, centralize integration, .spec naming (2026-08-04)
type: memory
tags: [testing, convention, vitest, coverage, session]
status: active
---

Test placement + naming convention (2026-08-04, user-directed): (1) Files use `.spec.ts(x)`, NOT `.test.ts(x)` — user: "not .test. but .spec.ts"; vitest discovers both by default, coverage exclude is `src/**/*.{test,spec}.{ts,tsx}`. (2) COLOCATE unit/component/hook tests next to their code; CENTRALIZE only true integration suites. Rationale (conceded to user): "behavior suite" framing was a granularity artifact — per-service units (injecting fabricated state/mocks) colocate fine; barrel-purity concern is a strawman since barrels enumerate exports explicitly (`export * from "./x.service"`) and never list test files. What genuinely stays centralized: cross-domain integration suites with no single home (card-parity.spec.ts = castCard→resolver→combat; commands.spec.ts = session→executeCommand→actions; history.spec.ts = session undo/redo) — these live in src/lib/game/__tests__/. Colocated: deck.services/deck.service.spec.ts, battle/services/{turn,state}.service.spec.ts, cards/{services/effects.service.spec.ts, data/schema.helper.spec.ts}, hooks/use-fish-mafia.strictmode.spec.tsx, components/game/*.spec.tsx. (3) Shared jsdom shims (canvas/ResizeObserver/scrollTo) extracted to src/components/game/test-utils.tsx (installJsdomShims() + renderGame()) — game-rendering specs import it; exclude test-utils.tsx from coverage. Coverage config: v8 provider, include src/lib/game+hooks+components/game, thresholds lines/stmts/funcs 75 branches 55 (gate: `npm run test:coverage` fails on regression). Caveat: game-level coverage fluctuates ±5% run-to-run because strictmode/economy specs drive a RANDOM opening hand — seed shuffle for a rock-solid gate.