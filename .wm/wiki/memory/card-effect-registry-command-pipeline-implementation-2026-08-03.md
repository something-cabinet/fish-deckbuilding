---
title: Card Effect Registry — Command Pipeline Implementation (2026-08-03)
type: memory
tags: [memory, session, engine, command-pattern, tdd, card-effect-registry]
status: active
---

Implemented spec wiki:specs/card-effect-registry (approved) end-to-end via wm-flow fast mode. 9 tasks, all done, verified on disk. Architecture shipped: (1) trusted JSON card packs (src/lib/game/cards/pack-01-starter.json) validated by zod schema (schema.ts, discriminatedUnion mirroring the TS CardEffect union via z.infer + _Equal compile-time drift guards, AC-16); (2) data-driven CardEffect union on CardDef (types.ts) with 7 kinds incl. custom escape hatch; (3) resolver (effects.ts) — exhaustive match reproducing the old castCard switch byte-identically (parity oracle tests 20/20), custom-handler registry with loud failure on unknown handlerId, zero handlers ship; (4) command base (commands.ts) — PlayerCommand data union + executeCommand dispatcher + CommandQueue with deterministic drain (AC-12); (5) GameSession (history.ts) — snapshot-based undo/redo (Memento), End Turn commits history (D10), enemy steps never enter undo stack; (6) hook (use-fish-mafia.ts) routes all player actions through the session queue with undo/redo exposed. Test harness added from scratch: vitest 4 (was none in the JS app), 57 tests green across 6 files (parity oracle, schema, resolver, commands, history, smoke), tsc clean, next build green. KEY LESSONS: (a) fixer lane silent-noop recurrence — fix-2 returned EMPTY with zero files (second documented occurrence of the critical-patterns noop); orchestrator-direct fallback worked and is faster for tightly-coupled engine refactors; (b) write behavior-capture tests FIRST against the current implementation (parity oracle) before refactoring — they make resolver parity provable; (c) task-status MCP API flaky with stale transitions — disk is source of truth. References: wiki:reference/design-patterns-gof, wiki:reference/sts2-architecture-command-pipeline.