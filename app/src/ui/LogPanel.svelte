<script lang="ts">type Props = { log: string[] }; let { log }: Props = $props(); const lines = $derived(log.slice(-6));</script>
<section class="log-panel" aria-label="Desk log"><h2>DESK LOG <span>LIVE</span></h2><div class="log-lines">{#if lines.length}{#each lines as line, index (`${index}-${line}`)}<p><i></i>{line}</p>{/each}{:else}<p><i></i>Console holding for instruction.</p>{/if}</div></section>
<style>.log-panel { width: 235px; pointer-events: auto; color: var(--ivory-1); } h2 { padding-bottom: 6px; border-bottom: 1px solid var(--steel); font-size: 9px; letter-spacing: .12em; } h2 span { color: var(--brass-light); font-size: 7px; } .log-lines { min-height: 82px; padding-top: 6px; mask-image: linear-gradient(to bottom, transparent, #000 12%, #000); } p { display: flex; gap: 6px; font-size: 9px; line-height: 1.45; } i { flex: 0 0 4px; height: 4px; margin-top: 4px; border-radius: 50%; background: var(--brass); }</style>
