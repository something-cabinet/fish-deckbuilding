<script lang="ts">import type { GameSnapshot } from '../engine/contract'; type Props = { snapshot: GameSnapshot; visible: boolean }; let { snapshot, visible }: Props = $props(); const output = $derived(JSON.stringify(snapshot, null, 2));</script>
{#if visible}<aside class="debug" aria-label="Engine snapshot"><header>SNAPSHOT / ENGINE READOUT</header><pre>{output}</pre></aside>{/if}
<style>.debug { position: fixed; right: 16px; top: 16px; z-index: 20; width: min(390px, 40vw); max-height: 48vh; overflow: auto; padding: 10px; border: 1px solid var(--steel); background: rgb(19 16 12 / .94); color: var(--ivory-2); font-size: 9px; pointer-events: auto; box-shadow: var(--shadow-lift); } header { color: var(--brass-light); margin-bottom: 7px; letter-spacing: .1em; } pre { white-space: pre-wrap; user-select: text; }</style>
