<script lang="ts">type Props = { visible: boolean; onDismiss: () => void }; let { visible, onDismiss }: Props = $props();</script>
{#if visible}<aside class="hint-strip"><div><b>OPERATING NOTE</b><span>Move via circular socket + chevron · Attack via split-ring · Pick a channel, drag to a live target · Sell with S · End turn with Space</span></div><button onclick={onDismiss} aria-label="Dismiss operating note">DISMISS</button></aside>{/if}
<style>.hint-strip { display: flex; align-items: center; justify-content: space-between; gap: 20px; max-width: 640px; padding: 8px 10px; border: 1px solid var(--steel); background: rgb(29 18 13 / .88); color: var(--ivory-1); pointer-events: auto; box-shadow: var(--shadow-lift); } div { display: flex; gap: 10px; align-items: baseline; } b { color: var(--brass-light); font-size: 8px; letter-spacing: .13em; white-space: nowrap; } span { font-size: 9px; line-height: 1.35; } button { border: 0; background: transparent; color: var(--ivory-0); cursor: pointer; font-size: 8px; letter-spacing: .08em; }</style>
